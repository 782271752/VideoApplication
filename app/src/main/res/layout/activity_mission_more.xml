<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/video_bottom_bg"
    tools:context=".MissionMoreActivity">

    <RelativeLayout
        android:background="@drawable/bg"
        android:layout_width="fill_parent"
        android:layout_height="50dp">

        <ImageButton
            android:padding="5dp"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:layout_centerVertical="true"
            android:src="@drawable/back"
            android:background="@drawable/search_btn"
            android:id="@+id/mission_more_back" />


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="新手任务"
            android:textColor="@color/white"
            android:layout_centerInParent="true"
            android:textSize="20sp" />

        <ImageButton
            android:scaleType="centerInside"
            android:layout_alignParentRight="true"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:padding="5dp"
            android:layout_centerVertical="true"
            android:src="@drawable/persion"
            android:background="@drawable/search_btn"
            android:id="@+id/mission_more_persion" />



    </RelativeLayout>

    <com.li.videoapplication.View.RefreshListView

        android:id="@+id/mission_more_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="@null"
        android:scrollbars="none"
        android:divider="@null"
        ></com.li.videoapplication.View.RefreshListView>

</LinearLayout>
